<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tm.board.review.dao.IBoardReviewDao">
  
<sql id="b_code">
	"r"
</sql>

	<insert id="insertBoardReview" parameterType="boardVo">
		insert into board (b_code, loc_category, sub_category, title, start_date, end_date, userid) values(#{b_code},#{b_loc_category},#{b_sub_category},#{b_title},#{b_start_date},#{b_end_date},#{userid})
	</insert>
	<insert id="insertImage" parameterType="imageVo">
		insert into image values(0, #{img_phy_name}, #{img_ori_name}, #{ima_code}, #{img_path});
	</insert>
	<insert id="insertContent" parameterType="contentsVo">
		insert into contents values(0, #{b_idx}, #{ct_num}, #{ct_contents});
	</insert>
	
	<update id="updateBoardReview" parameterType="boardVo">
		update board set b_code=#{b_code}, b_loc_category=#{b_loc_category}, b_title=#{b_title}, b_start_date=#{b_start_date}, b_end_date=#{b_end_date}, userid=#{userid} where b_idx=#{b_idx}
	</update>
	<update id="updateImage" parameterType="imageVo">
	      update image set img_phy_name=#{img_phy_name}, img_ori_name=#{img_ori_name}, img_path=#{img_path} where img_code=#{img_code}
	</update>
	<update id="updateContents" parameterType="contentsVo">
		update contents set ct_contents=#{ct_contents} where b_idx=#{b_idx}
	</update>
	
	<delete id="deleteBoardReview" parameterType="int">
		delete from board where b_idx=#{b_idx}
	</delete>
	
	<select id="selectReviewOne" parameterType="int" resultType="java.util.HashMap">
		select * from board b, image i where b.b_idx=#{b_idx} and b.b_idx=i.img_code;
	</select>
	
	<select id="selectReviewByCode" resultType="java.util.HashMap">
		select * from board b, image i where b.b_code=<include refid="b_code"></include> and  b.b_idx=i.img_code order by b.b_date desc
	</select>
	
	<select id="selectReviewByLocCategory" parameterType="String" resultType="java.util.HashMap">
		select b.b_title, b.b_userid, b.b_date i.img_path from board b, image i where b.b_code=<include refid="b_code"></include> and b.b_loc_category=#{b_loc_category} and  b.b_idx=i.img_code order by b.b_date desc
	</select>
	
	<select id="selectReviewBySubCategory"  parameterType="String" resultType="java.util.HashMap">
		select b.b_title, b.b_userid, b.b_date i.img_path from board b, image i where b.b_code=<include refid="b_code"></include> and b.b_sub_category=#{b_sub_category} and  b.b_idx=i.img_code order by b.b_date desc
	</select>
	
	<select id="selectReviewByLimit" parameterType="java.util.HashMap" resultType="boardVO">
		select * from board where b_code=<include refid="b_code"></include> order by b_idx desc limit #{skip},#{count}
	</select>
	
	<select id="getCountReview" resultType="int">
		select count(*) from board where b_code=<include refid="b_code"></include>
	</select>
	
	<select id="selectContents" parameterType="int" resultType="java.util.HashMap">
		select * from contents where b_idx=#{b_idx} order by ct_num
	</select>
</mapper>