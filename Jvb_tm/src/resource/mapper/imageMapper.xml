<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.IImageDao">
	<!-- 프로필 이미지 등록 -->
	<insert id="insertImageProfie" parameterType="java.util.HashMap">
		insert into image
		values(0, phy_name = #{phy_name}, ori_name = #{ori_name},
		code =
		#{code}, path = #{path})
	</insert>

	<insert id="insertImage">
		insert into image values(0,
		#{img_phy_name), #{img_ori_name},
		#{img_code}, #{img_path})
	</insert>

	<update id="updateImage">
		update image set
		img_phy_name=#{img_phy_name},
		img_ori_name=#{img_ori_name},
		img_path=#{img_path} where img_code=#{img_code}
	</update>

	<delete id="deleteImage">
		delete from image where img_code=#{img_code}
	</delete>


</mapper>